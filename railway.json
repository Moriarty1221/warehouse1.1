{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "nixPkgs": ["nodejs_20"],
      "buildCmd": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate",
      "startCmd": "cd backend && npx prisma db push --accept-data-loss && node src/seed.js && node src/index.js"
    }
  },
  "deploy": {
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
